<h1>Photogram</h1>

<%= link_to "New post", new_post_path %>

<div class="col-md-8">
  <% @posts.each do |post| %>
    <div>
      <%= image_tag( post.image.url(:medium) ) %>
    </div>
    <div>
      <%= post.caption %>
    </div>
  <% end %>
</div>
