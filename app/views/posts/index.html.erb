<h1>Photogram</h1>

<%= link_to "New post", new_post_path %>

<div class="col-md-8">
  <% @posts.each do |post| %>
    <div>
      <%= link_to image_tag( post.image.url(:medium) ), post_path(post[:id]) %>
    </div>
    <div>
      <%= post.caption %>
    </div>
  <% end %>
</div>
